[package]
name = "bilibili_login"
version = "0.1.0"
edition = "2024"

[dependencies]
min_hook_rs = "2.1"
windows-sys = { version = "0.60", features = [
    "Win32_Foundation",
] }
serde_json = "1"

[lib]
crate-type = ["cdylib"]